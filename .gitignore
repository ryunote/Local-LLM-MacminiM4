# ご指定の要件：プロジェクトフォルダ直下の history, images フォルダを除外
/history/
/images/

# =========================================================
# 以下は、今回の環境（Mac / AI開発）における推奨設定です。
# 必要に応じて残すか、不要であれば削除してください。
# =========================================================

# macOSの一時ファイル
.DS_Store
.AppleDouble
.LSOverride

# 環境変数ファイル（APIキーなどのシークレット情報が漏洩しないようにするため）
.env
*.env
.env.local

# Python関連（今後スクリプトなどを追加する場合）
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/